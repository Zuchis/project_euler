#ifndef UTILS_H
#define UTILS_H

#include <vector>
#include <array>
#include <iostream>
#include <algorithm>
#include <cmath>

std::vector<int> prime_sieve(int);
std::vector<int> prime_sieve_builder(std::vector<int>);

#endif
